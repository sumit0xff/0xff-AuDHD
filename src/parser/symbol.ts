import { SymbolTableEntry } from './types';

export class SymbolExtractor {
  public extract(tree: any, language: string, filePath: string): SymbolTableEntry[] {
    const symbols: SymbolTableEntry[] = [];
    return symbols;
  }
}
